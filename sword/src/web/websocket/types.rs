use socketioxide;

// Re-export socketioxide types directly
pub use rmpv::{Value, ValueRef};
pub use socketioxide::SocketIo;
pub use socketioxide::extract::{AckSender, Data, Event};

pub use socketioxide::extract::SocketRef;
