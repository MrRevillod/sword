# interceptors

This example demonstrates the use of interceptors in Sword for both HTTP and SocketIO adapters. It shows logging interceptors that log incoming requests and connections.

## Running the Example

1. Navigate to this directory: `cd examples/interceptors`
2. Run the application: `cargo run`

The server runs on port 8081. Make HTTP requests or connect via SocketIO to see the logs.